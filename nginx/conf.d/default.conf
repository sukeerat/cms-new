# ===========================================
# CMS Site Configuration
# ===========================================
# Replace YOUR_DOMAIN with your actual domain
# e.g., cms.example.com

# HTTP - Redirect to HTTPS (when SSL enabled)
server {
    listen 80;
    listen [::]:80;
    server_name YOUR_DOMAIN www.YOUR_DOMAIN;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect to HTTPS (uncomment when SSL is ready)
    # return 301 https://$server_name$request_uri;

    # Include the main location blocks (for HTTP-only mode)
    include /etc/nginx/snippets/locations.conf;
}

# HTTPS - Main server block (uncomment when SSL is ready)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name YOUR_DOMAIN www.YOUR_DOMAIN;
#
#     # SSL Certificates (Let's Encrypt)
#     ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#     ssl_trusted_certificate /etc/letsencrypt/live/YOUR_DOMAIN/chain.pem;
#
#     # HSTS
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     include /etc/nginx/snippets/locations.conf;
# }
