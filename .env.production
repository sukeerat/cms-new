# ===========================================
# CMS Production Environment Template
# ===========================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholders (YOUR_*) with real values
# 3. Generate secure passwords using: openssl rand -base64 32
#
# ===========================================

# ===========================================
# REQUIRED - Must Configure
# ===========================================

# Your domain name (e.g., cms.example.com)
DOMAIN=YOUR_DOMAIN

# Application URLs
FRONTEND_URL=https://YOUR_DOMAIN
CORS_ORIGIN=https://YOUR_DOMAIN
ALLOWED_ORIGINS=https://YOUR_DOMAIN,https://www.YOUR_DOMAIN

# JWT Secret - Generate with: openssl rand -base64 48
# CRITICAL: Change this to a random string!
JWT_SECRET=YOUR_JWT_SECRET_MINIMUM_32_CHARACTERS

# MongoDB Root Password - Generate with: openssl rand -base64 24
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=YOUR_MONGO_PASSWORD

# MinIO Password (min 8 chars) - Generate with: openssl rand -base64 24
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=YOUR_MINIO_PASSWORD

# ===========================================
# Application Settings
# ===========================================
NODE_ENV=production
LOG_LEVEL=warn
VERSION=latest

# API URL (relative for same-domain, or full URL if separate)
VITE_API_BASE_URL=/api
VITE_APP_NAME=CMS Portal
VITE_APP_ENV=production

# ===========================================
# Database Configuration
# ===========================================
# Connection string (uses Docker internal network)
DATABASE_URL=mongodb://cmsuser:cmspassword123@mongodb:27017/cms?authSource=cms
MONGO_DATABASE=cms

# ===========================================
# Cache Configuration
# ===========================================
REDIS_URL=redis://dragonfly:6379

# ===========================================
# JWT Settings
# ===========================================
JWT_EXPIRES_IN=24h

# ===========================================
# Rate Limiting
# ===========================================
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# ===========================================
# File Upload
# ===========================================
MAX_FILE_SIZE=10485760

# ===========================================
# MinIO Storage
# ===========================================
AWS_S3_BUCKET=cms-uploads
AWS_REGION=us-east-1

# ===========================================
# PM2 Configuration
# ===========================================
# 'max' = use all CPU cores, or specify number (e.g., 4)
PM2_INSTANCES=max

# ===========================================
# OPTIONAL - Configure as Needed
# ===========================================

# Email Configuration (Gmail example)
# For Gmail: Enable 2FA and create App Password
MAIL_USER=
MAIL_PASS=

# Google OAuth
# Create at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=https://YOUR_DOMAIN/api/auth/google/callback

# Firebase (for push notifications)
# Get from Firebase Console > Project Settings > Service Accounts
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=
FIREBASE_STORAGE_BUCKET=

# Cloudinary (for image processing)
# Get from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
